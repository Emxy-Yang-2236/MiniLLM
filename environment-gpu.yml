name: cs336-minillm
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - --index-url https://download.pytorch.org/whl/cu126
      - torch
      - --extra-index-url https://pypi.org/simple
      - numpy
      - pyyaml
      - regex
      - tiktoken
      - psutil
      - einops
      - jaxtyping
      - pytest-snapshot
      - pytest
